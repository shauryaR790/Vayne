# FastAPI backend — must be plain HTTP for local dev (no TLS on uvicorn).
# DO NOT put VAYNE_LLM_API_KEY or any OpenAI secret here.
# LLM keys belong only in the repo-root `.env` used by the Python backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
